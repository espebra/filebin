language: go

before_install:
 - sudo apt-get update -q
 - sudo apt-get install libvips15 curl

go:
  - 1.6

install:
  - make get-deps

script:
  - make check
  - make install

after_success:
  - source ./.travis/after_success
